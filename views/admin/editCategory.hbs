<div class="row justify-content-center">
    <div class="p-3 mt-5 col-md-9 col-lg-4 border border-primary" style="border-radius: 0.5rem">
        <div class="d-flex justify-content-center">
            <h2 class="pb-3">Edit category</h2>
        </div>
        <form action="/admin/categoryEdit/{{datas._id}}" method="post">

            <!-- Name field -->
            <div class="form-outline mb-3">
                <input type="text" id="form2Example2" name="category" class="form-control" placeholder="Enter category" value="{{datas.category}}" required />
                {{!-- <label class="form-label d-flex justify-content-center pt-2" for="form2Example2">Name</label> --}}
            </div>

            <!-- Submit button -->
            <button type="submit" class="btn btn-primary btn-block mb-4">Save changes</button>
        </form>
    </div>
</div>